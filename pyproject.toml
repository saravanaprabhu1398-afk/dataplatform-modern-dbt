[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dataplatform"
version = "0.1.0"
description = "Cloud-agnostic, plug-and-play data platform framework"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "typer",
    "pyyaml",
    "duckdb",
    "dbt-core==1.7.19",
    "networkx",
    "apscheduler",
    "fastapi",
    "httpx<0.28",
    "uvicorn",
    "snowflake-connector-python",
    "pandas",
    "requests",
    "sqlalchemy",
    "bcrypt>=4.0.0",
    # SQL lineage is parsed, not pattern-matched. Pinned to a major: parser
    # behaviour is the contract here, and it can change across majors.
    "sqlglot>=30,<31",
]
authors = [
    {name = "Prabhu Saravanan", email = "prabhu@example.com"},
]
maintainers = [
    {name = "Prabhu Saravanan", email = "prabhu@example.com"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.scripts]
dataplatform = "dataplatform.cli.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["dataplatform*"]

[tool.setuptools.package-data]
dataplatform = ["templates/*"]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "black",
    "flake8",
    "mypy",
    # Needed for tests/test_queue_leases_postgres.py, which exercises the
    # FOR UPDATE SKIP LOCKED claim path. Skipped unless
    # DATAPLATFORM_TEST_POSTGRES_URL is set.
    "psycopg2-binary",
]
ui = [
    "react",
    "d3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
